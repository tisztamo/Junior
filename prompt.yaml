task: prompt/task/doc/improve.md
attention:
  - docs/
  - docs/usage.md
requirements: >
  After "This will create the following files:" show only the files (dir was empty before junior init)!
  In "The prompt files will be gitignored.", mention
    - prompt.yaml, the prompt descriptor filled by the user (link to descriptor.md)
    - prompt.md, the generated prompt by Junior
    - change.sh, the changeset generated by the language model
os: OSX
