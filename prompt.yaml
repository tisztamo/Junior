task: prompt/task/bug/fix.md
attention:
  - src/init.js
requirements:
  - Factor out .gitignore creation to a new file in src/git/
  - Also gitignore node_modules/ if it is not already gitignored.
os: OSX
