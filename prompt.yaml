task: prompt/task/feature/implement.md
attention:
  - src/frontend/components/ExecuteButton.jsx
requirements: >
  The change text should be read from navigator.clipboard.readText if available,
  or from the current value of the input otherwise.
os: OSX
