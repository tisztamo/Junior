task: prompt/task/bug/fix.md
attention:
  - src/git/resetGit.js
requirements: |
  Resetgit correctly deletes every new files,
  and correctly leavddes prompt.yaml untouched,
  but it fails to reset other modified files.
