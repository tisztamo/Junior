task: prompt/task/feature/implement.md
attention:
  - ./
  - ./src/prompt/loadFormatTemplate.js
  - ./src/prompt/loadTaskTemplate.js
  - ./src/config.js
  - src/prompt/getSystemPromptIfNeeded.js
requirements: >
  When running from another dir, I get [Error: ENOENT: no such file or
  directory, open 'prompt/system.md']

  The system prompt should be loaded using the same logic that is used for the
  format prompt.

  Refactor the code so that 1. this logic is extracted to
  src/prompt/loadPromptFile.js and 2. getSystemPrompt is extracted to
  src/prompt/
format: prompt/format/shell.md
