task: prompt/task/refactor/custom.md
attention:
  - ./
  - src/
  - src/init.js
requirements: >
  Create src/command/init/copyDefaults.js
  and factor
  the generalized (src, dst) recursive defaults copying
  out.

  Rewrite the copy implementation to
  use nodejs fs, not exec.
os: OSX
