task: prompt/task/bug/fix.md
attention:
  - src/backend/handlers/generateHandler.js
  - src/frontend/components/GenerateButton.jsx
  - src/frontend/service/handleGeneratePrompt.js
  - src/frontend/generatePrompt.js
requirements: >
  In generateHandler, if error message starts with ENOENT, Respond with http
  status code 404.

  If error is different, respond with 500.

  On the frontend, in generatePrompt, throw if response code shows any error.
  Include the message from the error field.

  In handleGeneratePrompt catch errors from generatePrompt and alert the error
  message.
os: OSX
