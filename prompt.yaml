task: prompt/task/bug/fix.md
attention:
  - ./
  - src/
  - src/frontend/components/TasksList.jsx
requirements: >
  TaskList fails to set the initial value, because the parsed value is prefixed
  with "prompt/task/". Remove this prefix!
format: prompt/format/shell.md
