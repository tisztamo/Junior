task: prompt/task/doc/improve.md
attention:
  - README.md
requirements: >
  - Fix: The video caption is displayed on the same line as the video. Add an extra newline!

  - Extend the description in readme with the following (put it between the two
  paragraphs, reword if needed): Isn't that already possible with ChatGPT? No,
  LLMs have very limited "working memory", so it is not possible to directly
  work with them on large codebases.

  - Remove port info from README and link doc/web.md for more info

  heredoc whole README.md!
os: OSX
