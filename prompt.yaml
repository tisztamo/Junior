task: prompt/task/feature/implement.md
attention:
  - src/backend/handlers/generateHandler.js
  - src/prompt/processPrompt.js
  - src/prompt/createPrompt.js
  - src/prompt/getSystemPromptIfNeeded.js
  - src/frontend/generatePrompt.js
requirements: >
  When generating the prompt from the web, send "systemPrompt": true with the
  request. Add a force=false argument to getSystemPromptIfNeeded Make sure the
  handler forwards the systemPrompt value to processPrompt. Create a new argument "forceSystemPrompt = false" for processPrompt as its _second_
  argument. Also update createPrompt to forward the value.
os: OSX
