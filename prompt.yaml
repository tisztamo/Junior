task: prompt/task/feature/implement.md
attention:
  - src/frontend/tailwind.config.cjs
  - src/frontend/components/NavBar.jsx
requirements: >
  Create a theme switcher component (default from the OS, or dark if not
  available) Put it to the top right of the navbar.
os: OSX
