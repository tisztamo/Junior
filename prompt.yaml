task: prompt/task/feature/implement.md
attention:
  - src/prompt/createProjectSpecifics.js
  - src/init.js
  - ./
  - ./prompt/
requirements: >
  We need defaults to fill the prompt/ dir of initialized repos. Create
  prompt/defaults/ and recursively copy every file from it to the newly
  created repo on init. Eliminate createProjectSpecifics.
os: OSX
