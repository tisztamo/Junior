task: prompt/task/feature/implement.md
attention:
  - src/frontend/fetchTasks.js
requirements: >
  The server port is now configurable, update the client!
  Default port changed to 10101.
  Create frontend/getBaseUrl.js!
  When the user adds baseUrl=http://server:port to the query string, use that.
format: prompt/format/shell.md
