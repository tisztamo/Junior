task: prompt/task/bug/fix.md
attention:
  - ./
  - ./prompt/
  - src/init.js
  - package.json
requirements: >
  When junior-init is executed on a newly created repo, prompt files have to be
  correctly located. They are in the installed version of Junior, and inside
  there in prompt/defaults/.
os: OSX
