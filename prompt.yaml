task: prompt/task/feature/implement.md
attention:
  - src/frontend/components/ExecuteButton.jsx
requirements: >
  Use a textarea instead of the text input to prevent losing newline characters.
  Make it looking single-row.
os: OSX
