task: prompt/task/doc/improve.md
attention:
  - docs/web.md
requirements: >
  Update the info:
  Run the application with `npx junior-web` from your project or with `npm start` from Junior itself.
os: OSX
