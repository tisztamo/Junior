task: prompt/task/refactor/split.md
attention:
  - ./
  - src/
  - package.json
  - src/server.js
  - src/servePromptDescriptor.js
requirements: >
  The backend needs its own directory.
  Move server.js to it and split it into two.
  Also move servePromptDescriptor.js.
format: prompt/format/shell.md
