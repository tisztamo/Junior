task: prompt/task/feature/implement.md
attention:
  - ./
  - src/
  - src/execute/
  - src/execute/extractCode.js
  - src/execute/executeCode.js
requirements: >
  1. The executed code should be written to ./change.sh before executing.
  2. While executing the code, I do not see output in the terminal. Forward it!
  3. executeCode.js is too big. We need to split in two logically.
format: prompt/format/shell.md
